function pickDessert() { //naming function
    var single = "vanilla cupcake"; //assigning value to variable
    var mult = "choclate cake";
    document.getElementById("Button_text").innerHTML = single; //returning chosen variable value
}

function savoryFood() { //naming function
    var salt = "pot roast";
    salt += " is a great alternative"; //concatenated string
    document.getElementById("button_2").innerHTML = salt; //returning chosen variable value
}
